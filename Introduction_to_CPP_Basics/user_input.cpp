#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    cout << a << " " << b << endl;

    // string input
    char ch[50];
    cin.getline(ch, 50); // with space
    cout << ch << endl;

    return 0;
}